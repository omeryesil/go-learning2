# Interfaces - Assignment 02

Requirements 

+ Create a program that reads the contents of a text file then prints its contents to the terminal 
  + File name should be passed as a command line argument
  
    
